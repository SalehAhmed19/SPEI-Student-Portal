import React from "react";
import { icons } from "../assets/assets";

export default function useIcons() {
  const Dashboard = icons.Dashboard;
  const Payment = icons.Payment;
  const DownloadForm = icons.Download;
  const Result = icons.Result;
  const Improve = icons.Improve;
  const Attendance = icons.Attendance;
  const Routine = icons.Routine;
  const User = icons.User;
  const Hoste = icons.Hoste;
  const BloodDonate = icons.BloodDonate;
  const Support = icons.Support;
  const Logout = icons.Logout;
  const Number = icons.Number;

  return {
    Dashboard,
    Payment,
    DownloadForm,
    Result,
    Improve,
    Attendance,
    Routine,
    User,
    Hoste,
    BloodDonate,
    Support,
    Logout,
    Number,
  };
}
